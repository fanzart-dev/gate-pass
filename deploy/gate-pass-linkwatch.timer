[Unit]
Description=Check the Gate Pass public link every minute

[Timer]
# The first check is a minute after boot: "the server came back but the link
# did not" is one of the ways this has failed.
OnBootSec=1min
OnUnitActiveSec=1min
AccuracySec=5s

[Install]
WantedBy=timers.target
